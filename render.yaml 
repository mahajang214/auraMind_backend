services:
  - type: web
    name: auraMind-backend
    env: node
    region: singapore
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: free
    envVars:
      - key: PORT
        value: 10000
